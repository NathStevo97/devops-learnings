[project]
name = "devops-learnings"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "zensical>=0.0.31",
]

[dependency-groups]
dev = [
    "ruff>=0.16.4",
]
