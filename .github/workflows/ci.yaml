name: devops-learnings ci

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read
  pages: write
  id-token: write
jobs:
  markdown-ci:
    defaults:
      run:
        shell: bash

    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    
    runs-on: ubuntu-latest

    steps:
    - name: Checkout the Repo
      uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

    - uses: DavidAnson/markdownlint-cli2-action@ce4853d43830c74c1753b39f3cf40f71c2031eb9 # v23
      name: markdown-lint - Certifications
      with:
        config: .markdownlint.json
        globs: 'docs/certifications/*.md'

    - uses: DavidAnson/markdownlint-cli2-action@ce4853d43830c74c1753b39f3cf40f71c2031eb9 # v23
      name: markdown-lint - Tooling
      with:
        config: .markdownlint.json
        globs: 'docs/tooling/*.md'

  deploy-zensical:
    defaults:
      run:
        shell: bash
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    needs: markdown-ci
    runs-on: ubuntu-latest
    

    steps:

    - name: Configure Github Pages
      uses: actions/configure-pages@v6

    - name: Checkout the Repo
      uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

    - name: Install the latest version of uv and set the python version to 3.14
      uses: astral-sh/setup-uv@cec208311dfd045dd5311c1add060b2062131d57 # v8.0.0
      with:
        python-version: 3.14

    - name: Install Dependencies with uv
      run: uv sync

    - name: Build Site
      run: uv run zensical build --clean

    - name: Upload artifact for deployment
      uses: actions/upload-pages-artifact@7b1f4a764d45c48632c6b24a0339c27f5614fb0b # v4
      with:
        path: site

    - name: Deploy Pages
      uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5
      id: deployment
